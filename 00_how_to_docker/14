docker run -d --name lair -p 8080:80 --link spawning-pool:mysql wordpress

# https://docs.docker.com/samples/library/mysql/
# --link : container linking makes the MySQL instance available to other application containers.
