docker run --name Abathur -v /home/docker:/root -p 3000:3000 python:2-slim

# https://docs.docker.com/samples/library/python/
# -w : The default working directory for running binaries within a container is the root directory (/), but the developer can set a different default with the Dockerfile WORKDIR command. 
