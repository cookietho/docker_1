docker run -d --name overlord -p 5000:80 --restart always nginx

# docker pull nginx FIRST


# -d run container in the background.
# --name assign a container name.
# --restart flag on Docker run can specify a restart policy for how a container should or should not be restarted on exit.  "always restart the container regardless of the exit status.
# This will run the redis container with a restart policy of always so that if the container exits, Docker will restart it.
# -p : Publish all exposed ports to the host interfaces.
